<!DOCTYPE html>
<html>
  <head>
    <title>FacebookClone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav>
      <div class="nav-left">
          <%= link_to "Facebook", root_path, class: "logo" %>
      </div>
      <div class="nav-right">
      <% if user_signed_in? %>
        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-option" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "nav-option" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "nav-option" %>
      <% end %>
      </div>
    </nav>
    
    <%= yield %>
  </body>
</html>
